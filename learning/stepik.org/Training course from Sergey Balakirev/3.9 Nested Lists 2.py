# Three lines of the poem are introduced (each on a new line). Save it as a nested list broken down by lines and words
# (words are separated by a space).

a = list(map(str, input().split()))
b = list(map(str, input().split()))
c = list(map(str, input().split()))

a_list = [a, b, c]

print(a_list)
