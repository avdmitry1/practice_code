# A string of integers separated by a space is entered. If the first number is not equal to the last, then you need to
# add the True value, otherwise, the False value. Display the resulting list

# a_list = list(map(int, input().split()))

# a_list.append(a_list[0] != a_list[-1])

# print(*a_list)